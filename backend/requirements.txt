# OllamaLens Backend Dependencies v3
# Install: pip install -r requirements.txt

# ── Web Framework ──────────────────────────────────────────────────
fastapi[all]==0.111.0
uvicorn[standard]==0.29.0

# ── HTTP Client (used for Ollama AND tool API calls) ───────────────
# httpx handles both the Ollama WebSocket and external API calls
# (DuckDuckGo, Open-Meteo, Wikipedia) in async context
httpx==0.27.0

# ── Database ───────────────────────────────────────────────────────
sqlmodel==0.0.19
aiosqlite==0.20.0
sqlalchemy[asyncio]==2.0.30

# ── Settings ───────────────────────────────────────────────────────
pydantic-settings==2.2.1

# ── Retry logic ────────────────────────────────────────────────────
tenacity==8.3.0

# ── JSON Schema validation (for /chat/structured) ──────────────────
jsonschema==4.22.0

# ── Dev / Testing ──────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6

# ── No new packages needed for tools! ─────────────────────────────
# All tool APIs used are free REST APIs called via httpx (already installed):
#   DuckDuckGo Instant Answer API — no key
#   Open-Meteo weather API        — no key
#   Wikipedia REST API            — no key
#   Calculator — Python stdlib math module
#   Date/Time  — Python stdlib datetime